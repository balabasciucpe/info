server:
  port: 2000
  
  
eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: ${EUREKA_SERVICE_SERVICE_HOST:http://:localhost:8761}/eureka/}  
  instance:
#    hostname: localhost
    preferIpAddress: true  
    

